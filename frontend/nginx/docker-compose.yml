version: '3.8'

services:
  nginx:
    image: nginx:stable
    container_name: nginx-frontend
    ports:
      - "8080:80"
    volumes:
      - ../dist:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: always
