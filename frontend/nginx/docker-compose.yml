version: '3.8'

services:
  nginx:
    image: nginx:stable
    container_name: my-nginx-site
    ports:
      - "8080:80"
    volumes:
      - ../dist:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: always
